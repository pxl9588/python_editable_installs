[build-system]
requires = [
    "setuptools >= 61.0.0",
]
build-backend = "setuptools.build_meta"

[project]
name = "a.b.server"
version = "0.0.1"
dependencies = [
]

[tool.setuptools.packages.find]
where = ["src"]
